# Kiosk build: everything served locally, relative paths so it works loaded
# from disk rather than a web root.
VITE_BASE_URL=./
VITE_IMAGE_BASE=./images
# Shorter than review: an unattended floor machine should drop the popup soon
# after a visitor walks away. Seconds; 0 disables.
VITE_IDLE_TIMEOUT=120
